export default function ({ store, route, redirect }) {
  // if (store.state.auth.user && route.name === 'login') {
  //   redirect('/')
  // } else if (!store.state.auth.user && route.name !== 'login') {
  //   redirect('/login')
  // }
}
